import QtQuick 2.6
import org.kde.kirigami 1.0 as Kirigami

Component {
    id: contactsPage
    Kirigami.ScrollablePage {
        title: "Contacts"
        background: Rectangle {
            anchors.fill: parent
            color: "blue"
        }

    }
}
